# Locate trtexec (usually in this path on JetPack 4.4)
/usr/src/tensorrt/bin/trtexec \
  --onnx=strip_detector.onnx \
  --saveEngine=strip_detector.engine \
  --fp16 \
  --workspace=1024 \
  --explicitBatch


/usr/src/tensorrt/bin/trtexec \
  --onnx=strip_detector_updated.onnx \
  --saveEngine=strip_detector_updated.engine \
  --fp16 \
  --explicitBatch


/usr/src/tensorrt/bin/trtexec \
  --onnx=strip_detector_nano.onnx \
  --saveEngine=strip_detector_nano.engine \
  --explicitBatch \
  --fp16 \
  --workspace=512